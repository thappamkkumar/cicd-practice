name: CI Practice

on:
  push:

jobs:
  health:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: start
        run: echo "Workflow has started."

      - name: completed
        run: echo "Workflow has completed" 
